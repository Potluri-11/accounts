name: Accounts CI

on:
  push:
    branches:
      -  main

jobs:
  call-central-ci:
    uses:
YOUR_GITHUB_USERNAME/ central-ci/ .github/ workflows/ secure-ci.yml@main
    with:
      service_name: Accounts
